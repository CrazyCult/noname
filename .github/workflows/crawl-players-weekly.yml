name: Weekly Crawl Players

on:
  schedule:
    # Jeudi 3h00 Paris (02:00 UTC hiver / 01:00 UTC Ã©tÃ©)
    - cron: '0 2 * * 4'
  workflow_dispatch:

jobs:
  crawl-players-weekly:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment: Crawling

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Test DB connection
        run: |
          node -e "
          const mysql = require('mysql2/promise');
          async function test() {
            const conn = await mysql.createConnection({
              host: process.env.DB_HOST,
              user: process.env.DB_USER,
              password: process.env.DB_PASSWORD,
              database: process.env.DB_NAME,
              port: Number(process.env.DB_PORT),
              ssl: { rejectUnauthorized: true }
            });
            const [rows] = await conn.execute('SELECT 1 as ok');
            console.log('âœ… DB connected:', rows[0]);
            await conn.end();
          }
          test().catch(e => { console.error('âŒ DB error:', e.message); process.exit(1); });
          "
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_PORT: ${{ secrets.DB_PORT }}

      - name: Crawl Players (weekly)
        run: npm run crawl:players
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_PORT: ${{ secrets.DB_PORT }}
          MFL_API_URL: ${{ secrets.MFL_API_URL }}

      - name: Store absolute player snapshots
        run: npm run crawl:snapshots
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_PORT: ${{ secrets.DB_PORT }}
